---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';
import { formatDate } from '../../utils/date';
import { getReadingTime } from '../../utils/readingTime';

const posts = await getCollection('blog');
const sortedPosts = posts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout title="Blog - Kamil Rogozinski" description="Read my latest blog posts about web development, design, and technology.">
  <Header />
  <main class="min-h-screen pt-16 pb-20 px-5 sm:px-8">
    <div class="container mx-auto max-w-5xl">
      <header class="mb-16">
        <div class="font-mono text-[11px] text-accent tracking-[0.12em] mb-4">/blog</div>
        <h1 class="text-4xl md:text-5xl font-bold text-ink tracking-tight mb-4">Blog</h1>
        <p class="text-muted text-sm">Thoughts on software development, design, and technology.</p>
      </header>

      {sortedPosts.length > 0 ? (
        <div class="space-y-6">
          {sortedPosts.map((post) => (
            <article class="brutal-card group">
              <a href={`/Personal-Website/blog/${post.slug}`} class="block">
                <div class="flex flex-wrap gap-2 mb-4">
                  {post.data.tags?.map((tag: string) => (
                    <span class="brutal-tag">{tag}</span>
                  ))}
                </div>

                <h2 class="text-xl lg:text-2xl font-bold text-ink tracking-tight mb-3 group-hover:text-accent transition-colors">
                  {post.data.title}
                </h2>

                <p class="text-muted text-sm mb-6 leading-relaxed line-clamp-2">
                  {post.data.description}
                </p>

                <div class="brutal-hr mb-4"></div>

                <div class="flex flex-wrap items-center gap-4 font-mono text-xs text-faint">
                  <span>{post.data.author}</span>
                  <span class="text-dot">|</span>
                  <time datetime={post.data.pubDate.toISOString()}>
                    {formatDate(post.data.pubDate)}
                  </time>
                  <span class="text-dot">|</span>
                  <span>{getReadingTime(post.body)} min read</span>
                </div>
              </a>
            </article>
          ))}
        </div>
      ) : (
        <div class="flex flex-col items-center justify-center py-20">
          <div class="text-center max-w-2xl">
            <h2 class="text-2xl font-bold text-ink tracking-tight mb-4">No Posts Yet</h2>
            <p class="text-muted text-sm">
              Working on some exciting blog posts about web development, design, and technology.
            </p>
          </div>
        </div>
      )}
    </div>
  </main>
  <Footer />
</Layout>
