---
---

<nav class="fixed top-0 w-full bg-brutal-black z-[100] py-4 px-8 border-b border-brutal-rule">
  <div class="container mx-auto flex justify-between items-center relative">
    <a href="/Personal-Website/" class="text-xl font-bold text-brutal-white relative z-[102] tracking-tight">
      <span class="text-brutal-red">K</span>AMIL_R.
    </a>

    <!-- Status badge -->
    <div class="hidden lg:flex items-center gap-2 text-xs uppercase tracking-widest text-brutal-grey">
      <span class="status-dot"></span>
      Available
    </div>

    <!-- Desktop Navigation -->
    <div class="hidden lg:flex gap-8">
      <a href="/Personal-Website/#about" data-nav-section="about" class="text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors">About</a>
      <a href="/Personal-Website/#work" data-nav-section="work" class="text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors">Work</a>
      <a href="/Personal-Website/#blog" data-nav-section="blog" class="text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors">Blog</a>
      <a href="/Personal-Website/#contact" data-nav-section="contact" class="text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors">Contact</a>
    </div>

    <!-- Hamburger Menu Button -->
    <button
      id="hamburger"
      class="lg:hidden flex flex-col justify-center items-center w-10 h-10 relative focus:outline-none z-[102] gap-1"
      aria-label="Menu"
      aria-expanded="false"
    >
      <div class="menu-line"></div>
      <div class="menu-line"></div>
      <div class="menu-line"></div>
    </button>

    <!-- Mobile Navigation Menu -->
    <div
      id="mobile-menu"
      class="fixed left-0 right-0 top-[65px] bg-brutal-black border-b border-brutal-rule z-[101] lg:hidden"
      aria-hidden="true"
      inert
    >
      <div class="flex flex-col px-8 py-4">
        <a href="/Personal-Website/#about" data-nav-section="about"
          class="py-3 text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors border-b border-brutal-rule">
          About
        </a>
        <a href="/Personal-Website/#work" data-nav-section="work"
          class="py-3 text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors border-b border-brutal-rule">
          Work
        </a>
        <a href="/Personal-Website/#blog" data-nav-section="blog"
          class="py-3 text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors border-b border-brutal-rule">
          Blog
        </a>
        <a href="/Personal-Website/#contact" data-nav-section="contact"
          class="py-3 text-xs uppercase tracking-widest text-brutal-grey hover:text-brutal-red transition-colors">
          Contact
        </a>
      </div>
    </div>
  </div>
</nav>

<script>
  import { initMobileMenu } from '../scripts/header.js';

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initMobileMenu, { once: true });
  } else {
    initMobileMenu();
  }
</script>

<script>
  function initScrollSpy() {
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('[data-nav-section]');

    if (!sections.length || !navLinks.length) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;

          const sectionId = entry.target.id;
          navLinks.forEach((link) => {
            if (link.dataset.navSection === sectionId) {
              link.classList.add('nav-active');
            } else {
              link.classList.remove('nav-active');
            }
          });
        });
      },
      {
        rootMargin: '-20% 0px -60% 0px',
        threshold: 0,
      }
    );

    sections.forEach((section) => observer.observe(section));
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initScrollSpy, { once: true });
  } else {
    initScrollSpy();
  }
</script>
