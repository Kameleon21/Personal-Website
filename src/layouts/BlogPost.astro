---
import Layout from './Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ReadingProgress from '../components/ReadingProgress.astro';
import TableOfContents from '../components/TableOfContents.astro';
import { formatDate } from '../utils/date';
import { getReadingTime } from '../utils/readingTime';
import '../styles/blog-post.css';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  author: string;
  post: any;
}

const { title, description, pubDate, author, post } = Astro.props;
const { Content, headings } = await post.render();
const readingTime = getReadingTime(post.body);
---

<Layout title={title} description={description}>
  <ReadingProgress />
  <Header />

  <div class="min-h-screen">
    <div class="max-w-7xl mx-auto px-4 py-20">
      <div class="flex gap-12">
        <!-- Table of Contents - Left Sidebar -->
        <TableOfContents headings={headings} />

        <!-- Main Content -->
        <article class="flex-1 max-w-3xl mx-auto xl:mx-0">
          <!-- Article Header -->
          <header class="mb-12">
            <h1 class="text-3xl lg:text-5xl font-bold mb-6 text-ink leading-tight tracking-tight">
              {title}
            </h1>
            <div class="flex flex-wrap items-center gap-4 text-faint font-mono text-xs">
              <span class="text-ink">{author}</span>
              <span class="text-dot">|</span>
              <time datetime={pubDate.toISOString()}>{formatDate(pubDate)}</time>
              <span class="text-dot">|</span>
              <span>{readingTime} min read</span>
            </div>
            <div class="brutal-hr mt-6"></div>
          </header>

          <!-- Article Content -->
          <div class="prose prose-lg prose-brutal max-w-none
            prose-headings:font-sans prose-headings:font-bold prose-headings:text-ink prose-headings:tracking-tight
            prose-h2:text-2xl prose-h2:mt-12 prose-h2:mb-6 prose-h2:pb-2 prose-h2:border-b prose-h2:border-dot
            prose-h3:text-xl prose-h3:mt-8 prose-h3:mb-4
            prose-p:text-ink/80 prose-p:leading-relaxed
            prose-a:text-accent prose-a:no-underline hover:prose-a:underline
            prose-strong:text-ink
            prose-code:text-accent prose-code:bg-white prose-code:px-1.5 prose-code:py-0.5 prose-code:font-mono prose-code:text-sm prose-code:border prose-code:border-dot prose-code:rounded
            prose-pre:bg-ink prose-pre:border-2 prose-pre:border-ink prose-pre:rounded-lg
            prose-blockquote:border-l-accent prose-blockquote:bg-white/70 prose-blockquote:py-1 prose-blockquote:px-6
            prose-ul:text-ink/80 prose-ol:text-ink/80
            prose-li:marker:text-accent
            prose-hr:border-dot
          ">
            <Content />
          </div>
        </article>

        <!-- Spacer for right side balance on xl screens -->
        <div class="hidden xl:block w-60"></div>
      </div>
    </div>
  </div>

  <Footer />
</Layout>

<script src="../scripts/copy-code.js"></script>
